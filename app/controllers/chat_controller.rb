# The in-app conversation thread (.plans/mobile/08) — the Chat tab's target.
# Phase 0 stub: empty thread; the capture pipeline lands with chat capture (Phase 2).
class ChatController < AppController
  def show
  end
end
