# "Mais" — the native shells' fifth tab: the sidebar destinations that didn't get a tab.
# Rendered for web too (reachable only by URL) — no native-only conditionals.
class MenuController < AppController
  def show
  end
end
