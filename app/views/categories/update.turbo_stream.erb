<% if @saved %>
  <%= turbo_stream.replace dom_id(@category), partial: "categories/category", locals: { category: @category } %>
<% else %>
  <%= turbo_stream.replace dom_id(@category), partial: "categories/edit", locals: { category: @category } %>
<% end %>
