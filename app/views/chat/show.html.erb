<% content_for :title, t(".title") %>

<section class="mx-auto flex w-full max-w-2xl flex-col">
  <h1 class="font-display text-2xl font-extrabold tracking-tight"><%= t(".title") %></h1>

  <div id="chat_messages" class="mt-6 flex flex-col gap-1">
    <div class="rounded-box border border-base-300/60 bg-base-100 p-6 text-center text-sm text-base-content/60">
      <%= t(".empty") %>
    </div>
  </div>
</section>
