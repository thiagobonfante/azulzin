<%# Rendered by require_instrument into the entry sheet's entry_form frame in place of the add
    form when the account has no bank account or card yet. %>
<%= turbo_frame_tag "entry_form" do %>
  <%= render "shared/needs_instrument" %>
<% end %>
